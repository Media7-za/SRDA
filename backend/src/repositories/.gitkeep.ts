/**
 * Repositories Layer
 *
 * This directory contains all database access modules.
 * Repositories must:
 * - Handle all database interactions via Prisma Client
 * - Abstract the ORM from the rest of the application
 * - Be the ONLY layer that imports and uses Prisma Client
 * - Never contain business logic
 * - Never handle HTTP objects
 * - Never call external APIs
 */
